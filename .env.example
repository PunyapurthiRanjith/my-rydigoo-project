# Optional — only if you want OpenRouteService instead of the default OSRM routing.
# Free signup at https://openrouteservice.org/dev/#/signup (no credit card required).
# Leave empty to use OSRM + Photon (works out of the box, no keys needed).
VITE_OPENROUTESERVICE_API_KEY=
